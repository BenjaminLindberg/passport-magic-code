/**
 * Passport strategy for magic code authentication.
 *
 * @module passport-magic-code
 */
import Strategy from "./strategy";

/**
 * Export the Strategy constructor.
 *
 * @type {Strategy}
 */
exports = module.exports = Strategy;

/**
 * Export the Strategy constructor.
 *
 * @type {Strategy}
 */
exports.Strategy = Strategy;
